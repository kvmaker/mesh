# P00. 热路径日志降噪 — 详细分析

> 状态: `[x]` | 优先级: P1 | 预估工作量: 0.5 天 | 关联: 无

## 实施记录

- **分支**：`todo/p00-hotpath-log-denoise`
- **改动文件**：
  - `internal/server/tunnel/server.go`：新增包级 `debugPacket` 开关（读环境变量 `MESH_DEBUG_PACKET`，默认关闭）、`statsInterval = 30s` 常量；`TunnelServer` 加 `routeMiss`/`parseErr` 两个 `atomic.Uint64` 计数器；`Start` 额外启动 `statsLoop`；`routePacket` 与 `clientReadLoop` 的 route-miss / parse-error 从逐包 `log.Printf` 改为累加计数器 + 仅 debug 开启时打印。
  - `internal/server/tunnel/server_test.go`：`TestRoutePacketNoRoute`/`TestRoutePacketMalformed` 补计数器断言，新增 `TestDebugPacketDefaultOff` 守护「默认不逐包打印」不变量。
- **关键设计**：
  - 路径校正——P00 初稿引用的 `internal/tunnel/server.go:179` 的 `packet: len=%d dst=%s` 成功日志，在三分重组 + CodeRabbit 批次 `5bdb1c9`/`5b218df` 中已删除。本次收敛的是**残留的逐包错误/miss 日志**：`no route for %s`（client→client 与 TUN→client 两处）和 `packet parse error`。
  - 不改动 `config.Config` / YAML，用环境变量做 debug 开关，满足「不影响现有 CLI / API」。
  - `statsLoop` 只在计数有增量时打印聚合行 `[stats] route_miss=N (+d) parse_err=N (+d)`，空闲 server 保持静默；随 ctx 取消退出。
  - 连接级 / 低频日志（`write to TUN`、`write to client`、connect/disconnect、online/offline 更新失败）不在每包路径上，原样保留。
- **测试结果**：`go build ./...`、`go vet ./internal/server/tunnel/`、`go test -race ./...` 全部通过。

## 1. 背景与动机

当前 server 转发热路径中每个 IP 包都会打印一行日志，例 `internal/server/tunnel/server.go` 的 `log.Printf("packet: len=%d dst=%s", ...)`（三分重组前位于 `internal/tunnel/server.go`）。在 VPN 高包量场景下，这个日志会带来明显的：

- 格式化开销；
- 锁与 I/O 开销；
- systemd / journald 压力；
- 延迟抖动（写 stderr 阻塞）。

日志对排障是必要的，但默认不应该在热路径上每包打印。

## 2. 当前项目状态

关键文件：

- `internal/server/tunnel/server.go`：每个包都有 debug 级别日志。
- `internal/client/tunnel_client.go`：TUN/WS 主循环中没有日志，但仍受默认 logger 行为影响。

## 3. 实施方案

- 引入 debug 开关：
  - 环境变量 `MESH_DEBUG_PACKET=1`。
  - 或配置文件 `debug.packet_log`。
- 热路径仅在 debug 开启时打印。
- 周期性聚合日志（默认开启）：每 N 秒输出统计：
  ```text
  [stats] pkts=... bytes=... drops=... route_miss=...
  ```
- 错误日志（写失败、连接关闭、TUN 错误）保留原样。

**伪代码**

```text
if debug.packet_log:
    log.Printf("packet: ...")
else:
    stats.observePacket(len(pkt), dst)
```

## 4. 测试计划

- 单元/人工：debug 关闭时打 N 包，无 packet log。
- debug 开启时按预期打印。
- 周期性聚合日志频率符合配置。

## 5. 关联 TODO

- P02：减少热路径开销是异步转发队列前提。
- P04：聚合统计为可观测性提供数据。

## 6. 上线策略

- 默认行为：关闭 packet log。
- 排障时通过环境变量临时开启。
- 不影响现有 CLI / API。

## 7. 参考资料

- 当前实现：`internal/server/tunnel/server.go`
- 收敛后的热路径日志：`routePacket` / `clientReadLoop` 的 route-miss / parse-error 计数，`statsLoop` 聚合输出
